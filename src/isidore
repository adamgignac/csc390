#!/usr/bin/env python
from gui.mainwindow import MainWindow
from gi.repository import Gtk
from tools import constants
import os

if not os.path.isdir(constants.NOTES_DIR):
    os.mkdir(constants.NOTES_DIR)
m = MainWindow()
Gtk.main()
